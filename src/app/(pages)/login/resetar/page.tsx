import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Dogs | Resetar",
	description: "Resete sua senha",
};
export default async function ResetarPage() {
	return <div>Resetar</div>;
}
